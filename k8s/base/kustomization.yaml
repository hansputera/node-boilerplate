apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - sealed-secret.yaml
  - pdb.yaml
  - hpa.yaml

commonLabels:
  app: app-name
  managed-by: kustomize
